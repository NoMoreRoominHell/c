{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CMake: (MSVC) 调试 demo_app",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal"
        }
    ]
}