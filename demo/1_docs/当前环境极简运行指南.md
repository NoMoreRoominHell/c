Antigravity IDE + CodeLLDB (type: lldb) + CMake + MSVC 编译

微软官方打造的超级 C++ 包管理器 —— vcpkg

C:\vcpkg\vcpkg.exe install raylib:x64-windows

第 1 步：一键干掉历史残留缓存
Remove-Item -Recurse -Force build

第 2 步：正式以原生 Visual Studio 2026 生成解决方案配置
cmake -B build -G "Visual Studio 18 2026" -A x64

第 3 步：一键编译并执行你的 C 语言程序
cmake --build build --config Debug
.\build\Debug\demo_app.exe